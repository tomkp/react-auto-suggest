{
  "name": "react-auto-suggest",
  "description": "React auto-suggest component",
  "main": "dist/AutoSuggest.js",
  "version": "0.0.4",
  "repository": {
    "type": "git",
    "url": "https://github.com/tomkp/react-auto-suggest"
  },
  "author": "tomkp <tom@tomkp.com>",
  "keywords": [
    "react",
    "react-component",
    "auto-suggest"
  ],
  "dependencies": {
    "react": "^0.13.0-rc1"
  },
  "devDependencies": {
    "babel-core": "^4.6.6",
    "babel-jest": "^4.0.0",
    "babelify": "^5.0.4",
    "browserify": "^9.0.3",
    "jest-cli": "^0.4.0",
    "react-tools": "^0.13.0-rc1"
  },
  "scripts": {
    "test": "jest",
    "build": "browserify src/AutoSuggest.js -t -o dist/AutoSuggest.js",
    "demo": "browserify demo/Example.js -t -o demo/bundle.js"
  },
  "jest": {
    "rootDir": "./src",
    "scriptPreprocessor": "../preprocessor.js",
    "unmockedModulePathPatterns": [
      "react"
    ]
  },
  "browserify": {
    "transform": [
      [
        "babelify"
      ]
    ]
  }
}
